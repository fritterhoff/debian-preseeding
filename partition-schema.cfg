            
            1024 1024 1024 ext4                      \
                    $primary{ }                      \
                    $bootable{ }                     \
                    method{ format } format{ }       \
                    use_filesystem{ }                \
                    filesystem{ ext4 }               \
                    mountpoint{ /boot }              \
            .                                        \
            16384 16484 -1 $default_filesystem       \
                    $lvmok{ }                        \
                    lv_name{ root }                  \
                    method{ format }                 \
                    format{ }                        \
                    use_filesystem{ }                \
                    $default_filesystem{ }           \
                    mountpoint{ / }                  \
            .                                        \
            2048 2148 15360 $default_filesystem{ }   \
                    $lvmok{ }                        \
                    lv_name{ home }                  \
                    method{ format }                 \
                    format{ }                        \
                    use_filesystem{ }                \
                    $default_filesystem{ }           \
                    mountpoint{ /home }              \
            .                                        \
            16384 16484 30720 $default_filesystem{ } \
                    $lvmok{ }                        \
                    lv_name{ srv }                   \
                    method{ format }                 \
                    format{ }                        \
                    use_filesystem{ }                \
                    $default_filesystem{ }           \
                    mountpoint{ /srv }               \
            .                                        \
            16384 16484 30720 $default_filesystem{ } \
                    $lvmok{ }                        \
                    lv_name{ var }                   \
                    method{ format }                 \
                    format{ }                        \
                    use_filesystem{ }                \
                    $default_filesystem{ }           \
                    mountpoint{ /var }               \
            .                                        \
            16384 16484 30720 $default_filesystem{ } \
                    $lvmok{ }                        \
                    lv_name{ opt }                   \
                    method{ format }                 \
                    format{ }                        \
                    use_filesystem{ }                \
                    $default_filesystem{ }           \
                    mountpoint{ /opt }               \
            .                                        \
            4096 4196 4096 $default_filesystem{ }    \
                    $lvmok{ }                        \
                    lv_name{ tmp }                   \
                    method{ format }                 \
                    format{ }                        \
                    use_filesystem{ }                \
                    $default_filesystem{ }           \
                    mountpoint{ /tmp }               \
            .
